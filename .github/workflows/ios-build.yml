name: iOS Build and Deploy

on:
  push:
    branches: [ master, main ]
  workflow_dispatch:
    inputs:
      app_name:
        description: 'App name'
        required: true
        default: 'TaxSense'
      version:
        description: 'Version (e.g., 1.3.0)'
        required: true
        default: '1.3.0'
      build_number:
        description: 'Build number'
        required: true
        default: '78'

jobs:
  build-ios:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build web assets
        run: npm run build
      
      - name: Sync to iOS
        run: npx cap sync ios
      
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true
      
      - name: Install Fastlane
        run: gem install fastlane
      
      - name: Install CocoaPods
        run: |
          cd ios/App
          pod install --repo-update
          cd ../..
      
      - name: Decode API Key
        env:
          ASC_KEY_P8: ${{ secrets.ASC_KEY_P8 }}
          ASC_TEAM_ID: ${{ secrets.ASC_TEAM_ID }}
        run: |
          echo "$ASC_KEY_P8" | base64 -d > ios/AuthKey.p8
          ls -la ios/AuthKey.p8
          echo "Setting development team..."
          sed -i.bak "s/DEVELOPMENT_TEAM = \"\"/DEVELOPMENT_TEAM = \"$ASC_TEAM_ID\"/" ios/App/App.xcodeproj/project.pbxproj
          rm ios/App/App.xcodeproj/project.pbxproj.bak
          grep "DEVELOPMENT_TEAM" ios/App/App.xcodeproj/project.pbxproj | head -1
      
      - name: Build and Upload
        env:
          ASC_KEY_ID: ${{ secrets.ASC_KEY_ID }}
          ASC_ISSUER_ID: ${{ secrets.ASC_ISSUER_ID }}
          ASC_TEAM_ID: ${{ secrets.ASC_TEAM_ID }}
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
        run: |
          cd ios
          echo "Current directory: $(pwd)"
          echo "Files:"
          ls -la
          echo "Running fastlane..."
          fastlane build
